const nomes = ['bluno', 'pai de todos', 'duda', 'lary', 'day', 'miguelito', 'kauaUbranco', 'oto'];

let sala1= nomes.slice(0,nomes.length/2);
let sala2= nomes.slice(nomes.length/2);

console.log (sala1);
console.log (sala2);